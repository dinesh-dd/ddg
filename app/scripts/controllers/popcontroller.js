'use strict';

/**
 * @ngdoc function
 * @name gePantApp.controller:PopcontrollerCtrl
 * @description
 * # PopcontrollerCtrl
 * Controller of the gePantApp
 */
angular.module('gePantApp')
  .controller('PopcontrollerCtrl', function ($scope,$modalInstance) {
  	$scope.modalInstance = $modalInstance;
  });
